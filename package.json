{
  "name": "quiz-party-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "partykit dev",
    "build": "tsc",
    "predev": "npm run build",
    "deploy": "partykit deploy"
  },
  "dependencies": {
    "partykit": "^0.0.115"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}